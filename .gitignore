# Tiny Swords asset pack is NOT redistributable and is no longer used by
# the game, but keep it ignored in case it's ever downloaded locally again.
/Assets/Tiny Swords (Free Pack)/

# OS junk
.DS_Store
Thumbs.db

# Test harness output (regenerated locally when tools/harness.html runs)
tools/report.txt
tools/*.png
tools/shop_screen.html
