const ROOT = 'Assets/Kenney TD Pack/';

export function unitSpritePaths() {
  return {
    tank: `${ROOT}tank.png`,
    plane: `${ROOT}plane.png`,
    scout: `${ROOT}scout.png`,
    medic: `${ROOT}medic.png`,
  };
}

export function buildingSpritePaths() {
  return {
    tower: `${ROOT}tower.png`,
    hq: `${ROOT}hq.png`,
  };
}
